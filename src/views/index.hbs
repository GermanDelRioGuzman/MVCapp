<div class="container p-4">

    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center my-3">
                <div class="card-body">
                    <h3>En Casa</b></h3>
                </div>
            </div>  
        </div>
    </div>

    <div class="row">
        {{#if error}}
            <p>Error: {{error}}</p>
        {{/if}}

        {{#each data}}
            <div class="col-md-3">
                <div class="card">
                    <image src={{image}} alt="Image" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{name}}</h5>
                        <p class="card-text">{{species}}</p>
                        <a href= {{url}} class="btn btn-primary">Go</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>

</div>